# App that adds possibility to reduce quality of images in directory using windows explorer context menu
Quality was set to 70%, but you can set it to whatever you want in pyton file.
Quality 70% allow to reduce image size approx 4 times - -depending on image

## Installation
to install run as admin `install-run-as-admin.bat`
to add to context menu, run `add_context_menu.reg`